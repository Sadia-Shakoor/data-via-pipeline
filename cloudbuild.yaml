steps:
- name: 'gcr.io/cloud-builders/gcloud'
  args: ['functions', 'deploy', 'gitApp', '--region', 'YOUR_REGION', '--entry-point', 'hello_http', '--runtime', 'python39', '--trigger-http']

- name: 'gcr.io/cloud-builders/gcloud'
  args: ['functions', 'add-iam-policy-binding', 'gitApp', '--region', 'us-central1', '--member=allUsers', '--role=roles/cloudfunctions.invoker']


options:
  logging: CLOUD_LOGGING_ONLY

